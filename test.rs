struct Bidule {truc : i32}

fn machin(mut c : i32) -> i32 {
                        let mut alpha = Bidule {truc : 12};
                        return 3;
                        2
                       }

fn main() {let a = 14+4; 
					 if (a == 18) {print ! ("e\n");}
					 let y = (5 - a);
					 let mut z = 5%a;
					 z= 0;
					 let v = vec ! [true,false,false,true];
					 if (v[z] && v.len()<=12 || false)
					 	{
					 	let mut b=machin(5);
					 	}
					 else {;}
					 }

fn main() {let a = e.f;}
